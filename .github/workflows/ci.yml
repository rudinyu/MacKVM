name: CI

on:
  push:
  pull_request:

jobs:
  build-test-and-package:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Build, test, and package
        run: ./.codex/ci.sh

name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build-test-and-package:
    runs-on: macos-15
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - name: Build and test app bundles
        run: ./scripts/ci.sh
